from behave import then


@then(u'The average grade is displayed on the batch page')
def step_impl(context):
    raise NotImplementedError(u'STEP: Then The average grade is displayed on the batch page')
